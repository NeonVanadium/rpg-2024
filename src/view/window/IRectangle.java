package view.window;

// for custom objects that are functionally rectangles but don't need all the crazy constructors and methods java says
// rectangles need to have
public interface IRectangle {
	int getWidth();
	int getHeight();
	int getX();
	int getY();
}
